namespace ClubGrid.Models.Helper
{
    public class DisplayFormats
    {
        public const string DATE_DEFAULT = "{0:dd.MM.yyyy HH:mm}";
        public const string DATE_ONLY = "{0:dd.MM.yyyy}";
    }
}
